{% extends 'base.html.twig' %}

{% block title %}Arrazoia{% endblock %}

{% block body %}
    <h1>Arrazoia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arrazoium.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ arrazoium.name }}</td>
            </tr>
            <tr>
                <th>Aldibaterako</th>
                <td>{{ arrazoium.aldibaterako ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('arrazoia_index') }}">back to list</a>

    <a href="{{ path('arrazoia_edit', {'id': arrazoium.id}) }}">edit</a>

    {{ include('arrazoia/_delete_form.html.twig') }}
{% endblock %}
