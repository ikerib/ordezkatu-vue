{% extends 'base.html.twig' %}

{% block title %}EmployeeZerrendaType index{% endblock %}

{% block body %}
    <h1>EmployeeZerrendaType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Document</th>
                <th>Created</th>
                <th>Updated</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for employee_zerrenda_type in employee_zerrenda_types %}
            <tr>
                <td>{{ employee_zerrenda_type.id }}</td>
                <td>{{ employee_zerrenda_type.document }}</td>
                <td>{{ employee_zerrenda_type.created ? employee_zerrenda_type.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ employee_zerrenda_type.updated ? employee_zerrenda_type.updated|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('employee_zerrenda_type_show', {'id': employee_zerrenda_type.id}) }}">show</a>
                    <a href="{{ path('employee_zerrenda_type_edit', {'id': employee_zerrenda_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('employee_zerrenda_type_new') }}">Create new</a>
{% endblock %}
