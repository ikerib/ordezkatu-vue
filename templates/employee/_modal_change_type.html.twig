
<!-- Modal Import File -->
<div class="modal fade" id="modalChangeType" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Egoera aldatzea</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body modal-lg">
                {{ form_start(formEmployeeZerrendaType, {'attr': {'id': 'form_employee_zerrenda_type'}}) }}
                <p>Aukeratu zerrenda bat egoera zehazteko. Zerrendarik aukeratzen ez bada, zerrenda guztietan izango du eragina.</p>
                    {{ form_row(formEmployeeZerrendaType.zerrenda, { 'attr': { 'style': 'width: 100% !important'}}) }}
                    {{ form_row(formEmployeeZerrendaType.type, { 'attr': { 'style': 'width: 100% !important'}}) }}
                    {{ form_row(formEmployeeZerrendaType.documentFile) }}

                <div style="">
                    {{ form_row(formEmployeeZerrendaType.employee) }}
                </div>

                {{ form_end(formEmployeeZerrendaType) }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Itxi</button>
                <button type="button" class="btn btn-primary btnFormEmployeeZerrendaType">{{ "Ezarri" | trans }}</button>
            </div>
        </div>
    </div>
</div>
