{% extends 'base.html.twig' %}

{% block title %}Saila{% endblock %}

{% block body %}
    <h1>Saila</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saila.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ saila.name }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ saila.created ? saila.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ saila.updated ? saila.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('saila_index') }}">back to list</a>

    <a href="{{ path('saila_edit', {'id': saila.id}) }}">edit</a>

    {{ include('saila/_delete_form.html.twig') }}
{% endblock %}
