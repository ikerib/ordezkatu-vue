{#<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">#}
<nav class="navbar navbar-expand-md navbar-dark p-0 {% if is_granted('ROLE_PREVIOUS_ADMIN') %} bg-danger {% else %}bg-primary {% endif %}fixed-top">
    <a class="navbar-brand col-md-2" href="#">ORDEZKATU</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">

            </li>
        </ul>

        <div class="col-md-3">

        </div>
        {% if is_granted('ROLE_ADMIN') %}
            <div class="col-md-6">
                <input aria-label="Search" class="form-control mr-sm-2" id="inputMainSearchForm"
                       placeholder="bilatu Izena - Abizena - NAN - Telefonoa - Email" type="text">
            </div>
        {% endif %}


        <ul class="nav navbar-nav ezker-borobil">
            {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-info"  href="{{ path('admin_dashboard', {'_switch_user': '_exit'}) }}"><i class="fas fa-door-open"></i> &nbsp;{{ "Modu arruntera itzuli" | trans }}</a>
                </li>
            {% endif %}
            <li class="dropdown">
                <a href="#" class="nav-link dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-expanded="false">
{#                    Kaixo, {% if app.user['displayname'] is defined %} {{ app.user.displayname }} {% else %} {{ app.user.username }}{% endif %} <b class="caret"></b>#}
                </a>
                <div class="dropdown-menu dropdown-menu-right position-absolute" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">User submenu1</a>
                    <a class="dropdown-item" href="#">User submenu2</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">{{ "Saioa itxi" | trans }}</a>
                </div>
            </li>
        </ul>
    </div>
</nav>
