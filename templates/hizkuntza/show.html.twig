{% extends 'base.html.twig' %}

{% block title %}Hizkuntza{% endblock %}

{% block body %}
    <h1>Hizkuntza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hizkuntza.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hizkuntza.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hizkuntza_index') }}">back to list</a>

    <a href="{{ path('hizkuntza_edit', {'id': hizkuntza.id}) }}">edit</a>

    {{ include('hizkuntza/_delete_form.html.twig') }}
{% endblock %}
