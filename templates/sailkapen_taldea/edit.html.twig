{% extends 'base.html.twig' %}

{% block title %}Edit SailkapenTaldea{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">{{ sailkapen_taldea.name }}{{ " editatzen" }}</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group mr-2">
                <a class="btn btn-sm btn-outline-secondary" href="{{ path('sailkapen_taldea_index') }}">{{ "Itzuli zerrendara" | trans }}</a>

            </div>
            {#            <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle">#}
            {#                <span data-feather="calendar"></span>#}
            {#                This week#}
            {#            </button>#}
        </div>
    </div>

    {{ include('sailkapen_taldea/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
