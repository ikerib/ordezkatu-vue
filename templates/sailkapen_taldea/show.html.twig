{% extends 'base.html.twig' %}

{% block title %}SailkapenTaldea{% endblock %}

{% block body %}
    <h1>SailkapenTaldea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sailkapen_taldea.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sailkapen_taldea.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sailkapen_taldea_index') }}">back to list</a>

    <a href="{{ path('sailkapen_taldea_edit', {'id': sailkapen_taldea.id}) }}">edit</a>

    {{ include('sailkapen_taldea/_delete_form.html.twig') }}
{% endblock %}
