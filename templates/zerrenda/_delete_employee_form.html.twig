<form method="post" action="{{ path('employee_delete_zerrenda', {'employeeid': item.employee.id, 'zerrendaid': item.zerrenda.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.employee.id ~ item.zerrenda.id) }}">
    <a href="javascript:void(0);" class="btnRemoveRow text-danger" ><i class="fas fa-trash-alt"></i></a>
</form>
