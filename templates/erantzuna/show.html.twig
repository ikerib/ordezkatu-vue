{% extends 'base.html.twig' %}

{% block title %}Erantzuna{% endblock %}

{% block body %}
    <h1>Erantzuna</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ erantzuna.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ erantzuna.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('erantzuna_index') }}">back to list</a>

    <a href="{{ path('erantzuna_edit', {'id': erantzuna.id}) }}">edit</a>

    {{ include('erantzuna/_delete_form.html.twig') }}
{% endblock %}
